name: SALES_EDUCATION_APP
label: "Sales Education & Qualifier"
description: "Sales education and qualification tool for Data & AI solutions"

install:
  commands:
    - ["npm", "install", "--omit=dev"]

run:
  command: ["node", "server.js"]
